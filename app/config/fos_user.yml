# https://github.com/FriendsOfSymfony/FOSUserBundle/blob/master/Resources/doc/configuration_reference.md
fos_user:
    db_driver: orm # other valid values are 'mongodb', 'couchdb' and 'propel'
    firewall_name: main
    user_class: Management\UserBundle\Entity\User
    # emails https://github.com/FriendsOfSymfony/FOSUserBundle/blob/master/Resources/doc/emails.md
    service:
        mailer: fos_user.mailer.twig_swift
    from_email:
        address:        symfony@example.com
        sender_name:    Symfony example app
    resetting:
        email:
            # template: AcmeDemoBundle:User:resetting.email.html.twig
            from_email:
                address:        symfony@example.com
                sender_name:    Demo Resetting
    # registration:
    #     confirmation:
    #         from_email:
    #             address:        symfony@example.com
    #             sender_name:    Demo Registration

