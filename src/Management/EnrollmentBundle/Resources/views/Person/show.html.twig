{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{{ 'enrollment.person.details' | trans }}</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>{{ 'enrollment.person.name' | trans }}</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>{{ 'enrollment.person.email' | trans }}</th>
                <td>{{ entity.email }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="list-unstyled">
    <li>
        <a href="{{ path('person') }}">
            {{ 'button.back' | trans }}
        </a>
    </li>
</ul>

{% include 'ManagementEnrollmentBundle:Answer:_list.html.twig' with {'entities' : entity.answers} %}

{% endblock %}
