{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{{ 'enrollment.question.details' | trans }}</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>{{ 'enrollment.question.name' | trans }}</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>{{ 'enrollment.question.description' | trans }}</th>
                <td>{{ entity.helpblock }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="list-unstyled">
    <li>
        <a href="{{ path('question') }}">
            {{ 'button.back' | trans }}
        </a>
    </li>
</ul>

{% include 'OrganizationEnrollmentBundle:Answer:_list.html.twig' with {'entities' : entity.answers} %}

{% endblock %}
